<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>TasteTrail</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag "application" %> <!-- Include your main application stylesheet -->
  <%= yield(:head) %> <!-- Allow additional head content from specific views -->
</head>
<body>
  <%= render 'layouts/navbar' %>

  <div class="container">
    <% flash.each do |type, message| %>
      <div class="flash <%= type %>">
        <%= message %>
      </div>
    <% end %>

    <%= yield %>
  </div>

  <!-- Add a footer or other global content here if needed -->

  <%= javascript_include_tag "application" %> <!-- Include your main application JavaScript file -->
  <%= yield(:body) %> <!-- Allow additional body content from specific views -->
</body>
</html>
